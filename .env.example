# Copy to .env and adjust as needed.
# MLflow tracking store (local file store by default).
MLFLOW_TRACKING_URI=file:./mlruns

# Path the API loads the trained model from.
MODEL_PATH=models/model.pkl
